{
  "private": true,
  "scripts": {
    "bundle": "typings bundle . -o bundle",
    "test": "npm run bundle && ts-node test.ts"
  },
  "devDependencies": {
    "linkify-it": "^1.2.0",
    "ts-node": "^0.7.1",
    "typings": "^0.7.9"
  }
}
